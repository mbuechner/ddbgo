{{- if .Values.serviceAccounts.create }}
{{- $components := list
      (dict "name" (include "ddbgo.drupalServiceAccountName" .) "component" "drupal")
      (dict "name" (include "ddbgo.redisServiceAccountName" .) "component" "redis")
      (dict "name" (include "ddbgo.databaseServiceAccountName" .) "component" "db") -}}
{{- range $index, $item := $components }}
{{- if or (eq $item.component "drupal") (and (eq $item.component "redis") $.Values.redis.enabled) (and (eq $item.component "db") $.Values.database.enabled) }}
{{- include "ddbgo.assertAvailable" (dict "root" $ "apiVersion" "v1" "kind" "ServiceAccount" "name" $item.name) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $item.name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "ddbgo.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ $item.component }}
  annotations:
    {{- include "ddbgo.protectionAnnotations" $ | nindent 4 }}
    {{- with $.Values.serviceAccounts.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
automountServiceAccountToken: false
---
{{- end }}
{{- end }}
{{- end }}

